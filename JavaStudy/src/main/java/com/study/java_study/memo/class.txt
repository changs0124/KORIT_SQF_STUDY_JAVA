class - 최상위 자료형 / method : 클래스 안에 있는 함수
클래스(틀) :
객체(자료) : 틀을 통해 생성해 낼 수 있는것(사용 전) -> new 클래스(); : 찍어내기(주소)
인스턴스 : 클래스를 객체화 하는 것(사용을 하는 경우)

일반 자료형 : 이미 만들어진 자료형
참조 자료형 : 커스텀 가능(클래스, object객체)

메모리
스택(정적 메모리 영역) : 고정된 메모리 할당
힙(동적 메모리 영역) : 유연한 메모리 할당 -> 동적 메모리 할당 : 유연하게 메모리를 사용 가능
